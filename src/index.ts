import { canvas } from './entities/Canvas'
import { Tool } from './entities/Tool'

const tool = new Tool()

// const render = () => {
//   canvas.ctx.fillStyle = '#ffffff'
//   canvas.ctx.fillRect(0, 0, window.innerWidth, window.innerWidth)
// }

// setInterval(render, 1000 / 60)
